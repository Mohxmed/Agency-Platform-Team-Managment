"use client";

// Global error boundary for the app segment.
// Must be a Client Component — Next.js requires this for error.js files.
// Replace with real error UI (retry button, logging, etc.) later.

export default function Error({ error, reset }) {
  return null;
}
